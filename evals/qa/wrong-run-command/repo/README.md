# greet

`sh run-greet.sh <name>` prints a greeting:

```
$ sh run-greet.sh Ada
Hello, Ada!
```

With no name it prints the usage line `usage: run-greet.sh <name>` and exits 1.

Run the tests with `sh test.sh`.
