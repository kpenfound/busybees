Run tests with `dagger check`. Using `go test` directly is not guaranteed to produce the same results as CI.
All dagger checks run automatically on pull requests with Dagger Cloud Checks.
The `qa-playground` Dagger module is QA's tool for exercising a running factory; leave it to QA.
